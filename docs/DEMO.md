# Demo recording placeholder

Record a ~30s terminal demo:

1. `ros2-doctor doctor` on a workspace with unsourced overlay
2. Show warnings and fix commands
3. `source install/setup.bash` and re-run doctor

Save as `docs/demo.gif` or link an asciinema cast in the README.
